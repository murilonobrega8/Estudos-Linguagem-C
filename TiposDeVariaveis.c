void main() {

    //Definindo uma variável
    int     a;
    float   b;
    char    c;

    //Passando Valores
    a = 5;
    b = 2.3;
    c = ;

    //Escrevendo na Tela
    printf("\nO valor da variável 'a' é: %d", a);
    printf("\nO valor da variável 'b' é: %.2f", b);

    //Lendo um valor
    scanf("%d", &a);
    scanf("%f", &b);

    //Escrevendo na Tela
    printf("\nO valor da variável 'a' é: %d", a);
    printf("\nO valor da variável 'b' é: %.2f", b);

    //Pausando
    system("pause");    

}